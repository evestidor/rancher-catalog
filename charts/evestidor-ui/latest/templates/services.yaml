apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}

spec:
  selector:
    app: {{ .Values.service.name }}

  ports:
    - name: http
      port: 8000
      targetPort: 80
  type: LoadBalancer
